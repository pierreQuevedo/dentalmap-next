{
  "name": "dentalmap-next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "auth:generate": "auth generate --output src/db/auth-schema.ts -y",
    "build": "next build",
    "check": "pnpm typecheck && pnpm lint && pnpm test && pnpm build",
    "codegen": "dotenv -e .env.local -- graphql-codegen --config codegen.ts",
    "codegen:deploy": "graphql-codegen --config codegen.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "dotenv -e .env.local -- drizzle-kit migrate",
    "db:migrate:deploy": "drizzle-kit migrate",
    "db:push:preview": "drizzle-kit push --force",
    "db:studio": "dotenv -e .env.local -- drizzle-kit studio",
    "dev": "next dev",
    "lint": "eslint .",
    "schema:pull": "scripts/schema-pull.sh",
    "start": "next start",
    "sync:ans": "dotenv -e .env.local -- tsx scripts/sync/sync-ans.ts",
    "sync:geo": "dotenv -e .env.local -- tsx scripts/sync/sync-geo.ts",
    "sync:geocode": "dotenv -e .env.local -- tsx scripts/sync/geocode-ban.ts",
    "sync:prothesistes": "dotenv -e .env.local -- tsx scripts/sync/import-prothesistes.ts",
    "sync:redirections": "dotenv -e .env.local -- tsx scripts/sync/redirections-ancien-site.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "next typegen && tsc --noEmit",
    "vercel-build": "if [ \"$VERCEL_ENV\" = \"preview\" ]; then pnpm db:push:preview; else pnpm db:migrate:deploy; fi && pnpm codegen:deploy && next build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/blob": "^2.8.0",
    "better-auth": "^1.7.4",
    "drizzle-orm": "^0.45.2",
    "graphql": "^16.14.2",
    "html-react-parser": "^6.1.7",
    "maplibre-gl": "^6.9.0",
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "resend": "^6.27.0",
    "zod": "^4.6.1"
  },
  "devDependencies": {
    "@graphql-codegen/cli": "^7.4.1",
    "@graphql-codegen/client-preset": "^6.1.3",
    "@graphql-typed-document-node/core": "^3.2.0",
    "@tailwindcss/postcss": "^4",
    "@types/geojson": "^7946.0.16",
    "@types/node": "^22.20.2",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "auth": "1.7.4",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.31.10",
    "eslint": "^9",
    "eslint-config-next": "16.3.4",
    "tailwindcss": "^4",
    "tsx": "^4.23.13",
    "typescript": "^5",
    "vitest": "^5.0.0"
  },
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22 <23"
  }
}
